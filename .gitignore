# Never commit secrets — this file must always stay local!
.env
token.json

# Local conversation archive (session export) — moves device-to-device via scp, not git
session-archive.json

# Local noise
node_modules/
*.tmp
.DS_Store
# CJ MCP third-party repo (node_modules)/mcp/cj-api-mcp/node_modules/
