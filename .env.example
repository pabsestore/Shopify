# Copy this file to `.env` and fill in your real values, then never
# commit `.env` itself (it is gitignored).
#
# Find these in the Shopify DEV DASHBOARD -> your app -> Settings:
#   - Client ID
#   - Client secret (starts with shpss_)
#
# SHOPIFY_API_VERSION = quarterly Shopify API release, e.g. 2026-01.
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
SHOPIFY_CLIENT_ID=your-client-id
SHOPIFY_API_SECRET=your-client-secret
SHOPIFY_API_VERSION=2026-07

# CJdropshipping MCP (mcp/cj-shopify.cjs) — optional but unlocks real product data.
# Generate a random key once and keep it:  node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
# Login itself happens via the MCP's login page (no API key needed for local mode).
TOKEN_ENCRYPT_KEY=your-random-hex-key
CJ_PLATFORM=1
CJ_LANGUAGE=en
CJ_CURRENCY=USD